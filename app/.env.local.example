# FastAPI backend URL (used by frontend for API calls)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to "true" to show "Dev login (no OAuth)" on the login page (local dev only)
# NEXT_PUBLIC_DEV_LOGIN_ENABLED=true
