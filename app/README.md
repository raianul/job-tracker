# Job Tracker — Frontend

Next.js 14 + Mantine 7 app. See the [project root README](../README.md) for setup and run instructions.

- **Local:** `npm install` then `npm run dev` (set `NEXT_PUBLIC_API_URL` in `.env.local`).
- **Build:** `npm run build`
